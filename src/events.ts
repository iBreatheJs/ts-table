function onClickHeader(col: string) {
    // default is sort by this col

}


//  actions

// event config
const events = [
    "onclick"
]
export const events_custom = {
    "header": {
        click: "some action"
    }
}
export const events_default = {
    "header": {
        click: "some action"
    }
}